# backend-basic-task
Backend Basic Task — Express &amp; MongoDB CRUD API  This project is a simple backend RESTful API built with Express.js and MongoDB using Mongoose. It implements full CRUD operations (Create, Read, Update, Delete) for a Student model with fields like name, registration, semester, and department. 
